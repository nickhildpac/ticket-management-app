# ============================================
# SECURITY WARNING: Never commit .env to git!
# ============================================
# Copy this file to .env and update with your values
# The .env file is automatically loaded by docker-compose

# ============================================
# Backend Configuration (FastAPI)
# ============================================
# IMPORTANT: Generate a secure JWT secret for production!
# Use: openssl rand -base64 32
JWT_SECRET=change-this-to-a-secure-random-string-at-least-32-characters-long

# FastAPI environment. Use "production" with a strong JWT_SECRET outside local development.
APP_ENV=local

# Database connection string (docker-compose overrides this for the backend service)
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/ticket_management

# Backend API port exposed by docker-compose
BACKEND_PORT=8081

# ============================================
# Frontend Configuration
# ============================================
# API server URL (for frontend Docker build-time configuration)
# In production, this should be your actual backend URL
FRONTEND_API_URL=http://localhost:8081

# ============================================
# Database Configuration
# ============================================
# PostgreSQL database name
POSTGRES_DB=ticket_management

# PostgreSQL user
POSTGRES_USER=postgres

# IMPORTANT: Change this password in production!
# Use: openssl rand -base64 16
POSTGRES_PASSWORD=postgres

# ============================================
# Monitoring Configuration
# ============================================
# Grafana admin password
# IMPORTANT: Change this in production!
GRAFANA_PASSWORD=admin

# ============================================
# Resource Limits (optional)
# ============================================
# Backend Memory Limit
BACKEND_MEMORY_LIMIT=512M
# Frontend Memory Limit
FRONTEND_MEMORY_LIMIT=256M
# PostgreSQL Memory Limit
POSTGRES_MEMORY_LIMIT=512M
