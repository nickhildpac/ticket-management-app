# Design reference

The full **Queue – Data Sanctuary** HTML mock (Tailwind CDN + MD3-style palette, Manrope, Material Symbols) was provided in the project plan / implementation chat.

This folder holds traceability metadata. Color names and hex values are implemented in:

- `frontend/tailwind.config.ts` — `theme.extend.colors` (MD3-style keys)
- `frontend/src/index.css` — `:root` / `.dark` HSL variables for shadcn/Radix
