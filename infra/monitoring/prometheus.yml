global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  # - "first_rules.yml"
  # - "second_rules.yml"

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  # The authoritative FastAPI backend does not expose /metrics yet.
  # Re-enable this job when instrumentation is added.
  # - job_name: 'ticket-backend'
  #   static_configs:
  #     - targets: ['backend:8081']
  #   metrics_path: '/metrics'
  #   scrape_interval: 15s
